package no.uib.cipr.rs.meshgen.grdecl;

/**
 * Dimensions in a 3D system. Declared as an enumeration here to make the code
 * more readable in cases where we loop over each dimension separately.
 * 
 * @author roland.kaufmann@cipr.uib.no
 */
enum Dimension {
    X,  // = 0
    Y,  // = 1
    Z;  // = 2
}
